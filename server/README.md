# Express App

This is a simple Express app that serves as a starting point for your project.

## Installation

1. Clone the repository.
2. Install the dependencies using `npm install`.

## Usage

To start the app, run the following command:

npm start
